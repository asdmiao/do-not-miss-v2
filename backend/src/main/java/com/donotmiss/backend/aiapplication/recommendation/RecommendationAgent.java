package com.donotmiss.backend.aiapplication.recommendation;

import com.donotmiss.backend.aiapplication.agent.Agent;

/** Semantic recommendation reasoning only; retrieval and tracing are collaborators. */
public interface RecommendationAgent extends Agent {
}
