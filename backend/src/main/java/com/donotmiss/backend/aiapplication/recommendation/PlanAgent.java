package com.donotmiss.backend.aiapplication.recommendation;

import com.donotmiss.backend.aiapplication.agent.Agent;

/** Goal-to-plan reasoning only; schedule conflict checks are tools/domain services. */
public interface PlanAgent extends Agent {
}
